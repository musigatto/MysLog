<resources>

    <!-- Tema para la SplashScreen -->
    <style name="Theme.MysLogApp.Splash" parent="Theme.SplashScreen">
        <!-- Tu icono para la splash -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_mys_log</item>
        <!-- Color de fondo -->
        <item name="windowSplashScreenBackground">@color/splash_background</item> <!-- material3 -->
        <!-- Duración mínima del splash (opcional) -->
        <item name="postSplashScreenTheme">@style/Theme.MysLogApp</item>
    </style>

    <!-- Tema principal -->
    <style name="Theme.MysLogApp" parent="android:Theme.Material.Light.NoActionBar">

    </style>

</resources>

